---
import Head from "@components/Head.astro";
import Header from "@components/Header.astro";
import Footer from "@components/Footer.astro";
import { SITE } from "@consts";

type Props = {
  title: string;
  description: string;
};

const { title, description } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <Head title={`${title} | ${SITE.NAME}`} description={description} />
  </head>
  <body>
    <div class="noise"></div>
    <div class="overlay"></div>
    <div class="sphere-projection"></div>
    <div class="animated-background">
      <!-- 100 estrellas realistas con vía láctea -->
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
    </div>
    <Header />
    <main>
      <slot />
    </main>
    <Footer />
    <script>
      // @ts-nocheck
      function initSphereTracking() {
        const projection = document.querySelector('.sphere-projection');
        if (!projection) return;
        
        let mouseX = window.innerWidth / 2;
        let mouseY = window.innerHeight / 2;
        
        function updateProjection() {
          const x = (mouseX / window.innerWidth) * 100;
          const y = (mouseY / window.innerHeight) * 100;
          projection.style.background = `radial-gradient(circle at ${x}% ${y}%, rgba(34, 197, 94, 0.15) 0%, rgba(34, 197, 94, 0.05) 30%, transparent 70%)`;
        }
        
        // Tracking del ratón - siempre activo con capture phase
        const handleMouseMove = (e) => {
          mouseX = e.clientX;
          mouseY = e.clientY;
          updateProjection();
        };
        
        // Reiniciar tracking después de cada click
        const handleClick = () => {
          updateProjection();
        };
        
        // Actualizar si cambia el tamaño de la ventana
        const handleResize = () => {
          updateProjection();
        };
        
        document.addEventListener('mousemove', handleMouseMove, true);
        document.addEventListener('click', handleClick, true);
        window.addEventListener('resize', handleResize);
        
        // Inicializar
        updateProjection();
        
        // Limpiar listeners anteriores antes de reinicializar
        return () => {
          document.removeEventListener('mousemove', handleMouseMove, true);
          document.removeEventListener('click', handleClick, true);
          window.removeEventListener('resize', handleResize);
        };
      }
      
      // Inicializar en carga
      let cleanup = initSphereTracking();
      
      // Función para inicializar el toggle
      function initCrtToggle() {
        const sphereToggle = document.querySelector('#sphere-effect');
        const projection = document.querySelector('.sphere-projection');
        
        if (!sphereToggle || !projection) return;
        
        // Recuperar estado guardado y aplicar
        const sphereState = localStorage.getItem('sphereEnabled') === 'true';
        if (sphereState) {
          projection.classList.add('active');
          sphereToggle.classList.add('active');
        } else {
          projection.classList.remove('active');
          sphereToggle.classList.remove('active');
        }
        
        // Manejar clicks en el botón (remover listeners antiguos primero)
        const handleToggleClick = () => {
          const isActive = projection.classList.toggle('active');
          sphereToggle.classList.toggle('active');
          localStorage.setItem('sphereEnabled', String(isActive));
        };
        
        sphereToggle.removeEventListener('click', handleToggleClick);
        sphereToggle.addEventListener('click', handleToggleClick);
        
        return handleToggleClick;
      }
      
      // Inicializar toggle en carga
      let toggleHandler = initCrtToggle();
      
      // Reinicializar después de cambios de página (Astro View Transitions)
      document.addEventListener('astro:after-swap', () => {
        if (cleanup) cleanup();
        cleanup = initSphereTracking();
        toggleHandler = initCrtToggle();
      });
    </script>
  </body>
</html>
